kind: pipeline
name: default

steps:
  - name: build-docs
    image: node:alpine
    commands:
      - apk add --no-cache git
      - npm install
      - npm run docs:build
      - cd docs/.vuepress/dist
      - ls && git commit -m 'deploy'
      - echo ${GH_TOKEN}
      - git push -f https://plmercereau:${GH_TOKEN}@github.com/platyplus/platyplus.github.io.git master
    settings:
      GH_TOKEN:
        from_secret: GH_TOKEN
    when:
      branch: master
