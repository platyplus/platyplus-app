kind: pipeline
name: default

steps:
  - name: build-docs
    image: node:alpine
    commands:
      - apk add --no-cache git
      - npm install vuepress
      - npm run docs:build
      - cd docs/.vuepress/dist
    when:
      branch: master
