name: Commit on master branch

on:
  push:
    branches:
      - master
# on:
#   pull_request:
#     branches:
#       - master
#   push:
#     branches:
#       - master

jobs:
  # TODO uncomment
  # packages-test:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v1
      # - uses: actions/setup-node@v1
      #   with:
      #     node-version: 12
      # - run: yarn install
      # TODO run tests
      
  docker-build:
    # TODO uncomment
    # needs: packages-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: 12
      - run: yarn install
      - run: yarn run build
      - run: yarn run ci:docker build
        env:
          IMG_PREFIX: gcr.io/platyplus-0402
      - run: yarn run ci:docker push
        env:
          IMG_PREFIX: gcr.io/platyplus-0402
      - uses: actions/gcloud/auth@master
        env:
          GCLOUD_AUTH: ${{ secrets.gcloud_auth }}
      - uses: "actions/gcloud/cli@master"
        with:
          args: config set project platyplus-0402 
      - uses: "actions/gcloud/cli@master"
        with:
          args: container clusters list
      - uses: "actions/gcloud/cli@master"
        with:
          args: "auth configure-docker --quiet
  # TODO deploy on staging
      
