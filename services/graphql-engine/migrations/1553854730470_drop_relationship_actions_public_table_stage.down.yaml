- args:
    name: actions
    table:
      name: stage
      schema: public
    using:
      foreign_key_constraint_on:
        column: stage_id
        table:
          name: encounter_type_action
          schema: public
  type: create_array_relationship
