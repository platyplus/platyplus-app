- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql:
      CREATE TABLE "public"."encounter_stage"("id" uuid NOT NULL UNIQUE DEFAULT
      public.gen_random_uuid(), "encounter_id" uuid NOT NULL, "state_id" uuid NOT NULL, PRIMARY
      KEY ("id") );
  type: run_sql
- args:
    name: encounter_stage
    schema: public
  type: add_existing_table_or_view
