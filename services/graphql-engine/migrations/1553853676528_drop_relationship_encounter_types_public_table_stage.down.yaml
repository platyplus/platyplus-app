- args:
    name: encounter_types
    table:
      name: stage
      schema: public
    using:
      foreign_key_constraint_on:
        column: stage_id
        table:
          name: encounter_type_stage
          schema: public
  type: create_array_relationship
